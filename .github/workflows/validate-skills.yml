name: Validate Skills

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      # ── A1: Secret scanning ────────────────────────────────────────────────
      - name: Scan for secrets
        run: |
          exit_code=0

          # Helper: print match and set failure flag
          fail_match() {
            echo "✗ Secret pattern found:"
            echo "$1"
            exit_code=1
          }

          # Exclude this workflow file and .git from all scans
          EXCLUDE="--exclude-dir=.git --exclude=validate-skills.yml"

          # API key prefixes
          for pattern in 'sk-[A-Za-z0-9]{20,}' 'ghp_[A-Za-z0-9]{36}' 'gho_[A-Za-z0-9]{36}' 'AKIA[0-9A-Z]{16}'; do
            matches=$(grep -rn $EXCLUDE -E "$pattern" . 2>/dev/null || true)
            [ -n "$matches" ] && fail_match "$matches"
          done

          # Generic assignment patterns (password/token/secret = '...' or "...")
          for pattern in "password\s*=\s*['\"][^'\"]{1,}" "token\s*=\s*['\"][^'\"]{1,}" "secret\s*=\s*['\"][^'\"]{1,}"; do
            matches=$(grep -rn $EXCLUDE -iP "$pattern" . 2>/dev/null || true)
            [ -n "$matches" ] && fail_match "$matches"
          done

          # Base64-ish keys: an '=' sign followed immediately by 40+ alphanumeric chars
          matches=$(grep -rn $EXCLUDE -E '=[A-Za-z0-9/+]{40,}' . 2>/dev/null || true)
          [ -n "$matches" ] && fail_match "$matches"

          exit $exit_code

      # ── A2: Internal reference scanning ───────────────────────────────────
      - name: Scan for internal references
        run: |
          exit_code=0

          fail_match() {
            echo "✗ Internal reference found:"
            echo "$1"
            exit_code=1
          }

          # Exclude CI workflow, git dir, design specs/plans (historical docs),
          # validation reports, and agent config docs from scanning
          EXCLUDE="--exclude-dir=.git --exclude-dir=superpowers --exclude-dir=ext_validation --exclude=validate-skills.yml"

          # ProGet (case-insensitive) — scan skills/ and install/ only
          matches=$(grep -rn $EXCLUDE -i 'ProGet' ./skills/ ./install/ ./CLAUDE.md ./CONTRIBUTING.md ./SECURITY.md 2>/dev/null || true)
          [ -n "$matches" ] && fail_match "$matches"

          # *.loginvsi.com but NOT www.loginvsi.com or docs.loginvsi.com
          matches=$(grep -rn $EXCLUDE -E '\.loginvsi\.com' ./skills/ ./install/ 2>/dev/null \
            | grep -v 'docs\.loginvsi\.com' \
            | grep -v 'www\.loginvsi\.com' || true)
          [ -n "$matches" ] && fail_match "$matches"

          # .claude/ path references — only flag in skills/ (not in README/install which legitimately reference it)
          matches=$(grep -rn $EXCLUDE -E '(~\/|\\|\/)\.claude\/' ./skills/ 2>/dev/null \
            | grep -iv 'warning\|note\|caution\|do not\|avoid\|common mistake' || true)
          [ -n "$matches" ] && fail_match "$matches"

          # Build tag pattern -LE-<digits> — scan skills/ and install/ only
          matches=$(grep -rn $EXCLUDE -E '\-LE\-[0-9]+' ./skills/ ./install/ 2>/dev/null || true)
          [ -n "$matches" ] && fail_match "$matches"

          # TFS references (case-insensitive) — scan skills/ and install/ only
          matches=$(grep -rn $EXCLUDE -iE '(^|[^a-zA-Z])(TFS|tfs\.)' ./skills/ ./install/ 2>/dev/null || true)
          [ -n "$matches" ] && fail_match "$matches"

          exit $exit_code

      # ── A3: File reference verification ───────────────────────────────────
      - name: Verify referenced files exist
        run: |
          exit_code=0
          for skill_md in skills/*/SKILL.md; do
            skill_dir=$(dirname "$skill_md")
            # Extract relative markdown links: [text](references/... or scripts/... or assets/...)
            links=$(grep -oP '\[^]]+\]\(\K(references|scripts|assets)/[^)]+' "$skill_md" 2>/dev/null || true)
            while IFS= read -r link; do
              [ -z "$link" ] && continue
              target="$skill_dir/$link"
              if [ ! -e "$target" ]; then
                echo "✗ $skill_md references missing file: $link"
                exit_code=1
              else
                echo "✓ $skill_md -> $link exists"
              fi
            done <<< "$links"
          done
          exit $exit_code

      # ── Existing: Validate all skills ─────────────────────────────────────
      - name: Validate all skills
        run: |
          exit_code=0
          for dir in skills/login-enterprise-*/; do
            echo "Validating $dir..."
            if npx skills-ref validate "$dir"; then
              echo "✓ $dir passed"
            else
              echo "✗ $dir failed"
              exit_code=1
            fi
            echo ""
          done
          exit $exit_code

      # ── Existing: Check SKILL.md body length (A5c fix applied) ───────────
      - name: Check SKILL.md body length
        run: |
          exit_code=0
          for skill_md in skills/*/SKILL.md; do
            # Count lines that belong to the body (after the second frontmatter ---).
            # Only a line consisting of exactly '---' and nothing else counts as a
            # frontmatter delimiter; we stop tracking after the second such occurrence.
            body_lines=$(awk '
              /^---[[:space:]]*$/ && delim_count < 2 { delim_count++; next }
              delim_count >= 2 { body++ }
              END { print body+0 }
            ' "$skill_md")
            if [ "$body_lines" -gt 500 ]; then
              echo "✗ $skill_md body is $body_lines lines (max 500)"
              exit_code=1
            else
              echo "✓ $skill_md body is $body_lines lines"
            fi
          done
          exit $exit_code

      # ── Existing: Check directory name matches SKILL.md name ──────────────
      - name: Check directory name matches SKILL.md name
        run: |
          exit_code=0
          for dir in skills/login-enterprise-*/; do
            dir_name=$(basename "$dir")
            skill_name=$(awk '/^name:/{print $2; exit}' "$dir/SKILL.md")
            if [ "$dir_name" != "$skill_name" ]; then
              echo "✗ Directory '$dir_name' does not match SKILL.md name '$skill_name'"
              exit_code=1
            else
              echo "✓ $dir_name matches"
            fi
          done
          exit $exit_code

      # ── A5b: Run Pester tests ──────────────────────────────────────────────
      - name: Run Pester tests
        shell: pwsh
        run: |
          Install-Module -Name Pester -MinimumVersion 5.0 -Force -Scope CurrentUser
          $testFiles = Get-ChildItem -Path skills/ -Recurse -Filter '*.tests.ps1'
          if ($testFiles.Count -eq 0) {
            Write-Host "No Pester test files found under skills/ — skipping."
            exit 0
          }
          $failed = 0
          foreach ($tf in $testFiles) {
            Write-Host "Running $($tf.FullName)..."
            $result = Invoke-Pester -Path $tf.FullName -PassThru
            if ($result.FailedCount -gt 0) { $failed += $result.FailedCount }
          }
          if ($failed -gt 0) { exit 1 }
